# Make the Footer Interesting

The "Read notes" button is a bit _boring_, and seems to convey an action (like create/delete) rather than a link to a page.

Let's turn it into a full-width clickable footer with a subtle separator.

Here's the design:

<callout-warning>TODO</callout-warning>
