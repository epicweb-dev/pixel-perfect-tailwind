# Container width

<EpicVideo url="https://www.epicweb.dev/workshops/full-stack-foundations/styling/bundling-css-in-remix/solution" />

We'll first focus on the container width.

### Things are spreading a bit too wide 👈 ----- 👉

Notice how wide the profile card currently is? The bio is a little hard to read as a result.

You've very likely heard about `line-height` many times, but did you know that line _length_ is almost equally important in text legibility and visual appeal?

Let's try to make the profile card a bit narrower. We can do this by adding a `max-width` to the `div` that contains the profile card.
