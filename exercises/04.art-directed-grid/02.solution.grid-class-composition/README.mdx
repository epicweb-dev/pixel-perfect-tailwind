# Grid Class Composition

Whew! That was actually pretty easy 😁

One thing to keep in mind: when you start placing items manually in your grid like that, any element that does not have a `grid-colum-start` or `grid-row-start` property will be find the first available cell in the flow of the grid, and occupy that!
