/* 
This file needs to be here as a "placeholder", because we set the path to the config file in the root `.prettierrc` file, so that `prettier-plugin-tailwindcss` works with the correct config file in every exercise app.
*/
